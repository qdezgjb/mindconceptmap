# MindGraph Docker Environment Configuration
# Copy this file to .env and update the values for your deployment

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Qwen API Key (Required - for core application functionality)
QWEN_API_KEY=your-qwen-api-key-here

# External Access Configuration (Important for Docker deployment)
# Set this to your server's public IP address for external access
# If not set, the system will try to auto-detect but may fail in Docker
EXTERNAL_HOST=your-public-ip-address

# ============================================================================
# QWEN API CONFIGURATION
# ============================================================================

# Qwen API URL (usually don't need to change)
QWEN_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions

# Model Selection Strategy:
# - qwen-turbo: For classification tasks (faster, cheaper)
# - qwen-plus: For generation tasks (higher quality)
QWEN_MODEL_CLASSIFICATION=qwen-turbo
QWEN_MODEL_GENERATION=qwen-plus

# API Parameters
QWEN_TEMPERATURE=0.7
QWEN_MAX_TOKENS=3000
QWEN_TIMEOUT=40

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# FastAPI + Uvicorn Configuration
HOST=0.0.0.0
PORT=9527
DEBUG=False

# Environment
MINDGRAPH_ENV=production

# ============================================================================
# GRAPH CONFIGURATION
# ============================================================================

# Language for graph generation
GRAPH_LANGUAGE=zh

# UI/Graph Styling
TOPIC_FONT_SIZE=18
CHAR_FONT_SIZE=14

# Watermark
WATERMARK_TEXT=MindGraph

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Available levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# DOCKER-SPECIFIC NOTES
# ============================================================================
#
# 1. EXTERNAL_HOST is crucial for Docker deployments
#    - Set it to your server's public IP address
#    - This ensures external clients can access the application
#    - Without it, the app may only work locally
#
# 2. HOST should remain 0.0.0.0 for Docker
#    - This allows the container to accept connections from outside
#
# 3. PORT should match the docker-compose.yml port mapping
#    - Default is 9527 (MindGraph's standard port)
#
# 4. For production deployments:
#    - Set DEBUG=False
#    - Use appropriate LOG_LEVEL (INFO or WARNING)
#    - Ensure QWEN_API_KEY is properly configured
#
